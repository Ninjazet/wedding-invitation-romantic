name: Deploy Romantic Wedding Invitation

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      pages: write
      id-token: write
      
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
      
    steps:
    - name: Checkout 💕
      uses: actions/checkout@v4
      
    - name: Setup Pages with Love 🌹
      uses: actions/configure-pages@v4
      
    - name: Upload romantic artifact 💖
      uses: actions/upload-pages-artifact@v3
      with:
        path: '.'
        
    - name: Deploy to GitHub Pages 💍
      id: deployment
      uses: actions/deploy-pages@v4